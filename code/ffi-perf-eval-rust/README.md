FFI Performance Evaluation
==========================

This repository implements a simple Rust FFI and evaluates how quickly it can be
called from Go code.

Installation
------------

To build and run the benchmark, type:

```
make
```
